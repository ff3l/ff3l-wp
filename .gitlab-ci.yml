before_script:
  - uname -a
  - sudo apt-get update -qq && sudo apt-get install -y -qq nodejs nodejs-legacy npm
  - sudo npm install -g grunt
  - npm install

build:
  script:
    - grunt
  artifacts:
    paths:
      - /